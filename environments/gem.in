# Minimal dependencies imported by liziniu/GEM train.py at the pinned commit.
# The official requirements list vllm==2.6.1, which does not exist on PyPI and
# is not imported by the training entrypoint. It is intentionally excluded.
torch==2.4.0
transformers==4.57.1
datasets==2.14.6
pyarrow==14.0.2
numpy==1.26.4
deepspeed==0.15.0
setuptools==83.0.0
accelerate==1.11.0
pydantic==2.13.4
pyyaml==6.0.3
sentencepiece==0.1.99
